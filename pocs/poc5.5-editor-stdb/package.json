{
  "name": "poc5.5-editor-stdb",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "generate": "spacetime generate --lang typescript --out-dir src/module_bindings --module-path server/spacetimedb"
  },
  "dependencies": {
    "spacetimedb": "^2.0.2",
    "three": "^0.171.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "typescript": "^5.7.0"
  }
}
